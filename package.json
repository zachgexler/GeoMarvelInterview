{
  "name": "geomarveltechint",
  "description": "Pandas",
  "version": "0.0.6",
  "dependencies": {
    "pandas": "0.0.3"
  },
  "devDependencies": {
    "react-scripts": "1.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}